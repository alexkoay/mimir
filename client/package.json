{
  "name": "mimir-ui",
  "version": "0.1.0",
  "description": "Dashboard client UI",
  "main": "src/base.js",
  "scripts": {
    "build": "npm-run-all -p build-js build-css",
    "build-js": "browserify base.ts -s mimir -o mimir.js -vd -p [ tsify --target es6 --noImplicitAny ] -t [ babelify --plugins transform-es2015-modules-commonjs --extensions .ts,.js ]",
    "build-css": "node-sass css/base.scss mimir.ui.css",
    "watch": "npm-run-all -p watch-js watch-css",
    "watch-js": "watchify base.ts -s mimir -o mimir.js -vd -p [ tsify --target es6 --noImplicitAny ] -t [ babelify --plugins transform-es2015-modules-commonjs --extensions .ts,.js ]",
    "watch-css": "node-sass -r --watch=css/ css/base.scss mimir.ui.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "author": "Alex Koay",
  "devDependencies": {
    "babel-core": "^5.8.33",
    "babel-loader": "^5.3.3",
    "ts-loader": "^0.6.1",
  }
}
